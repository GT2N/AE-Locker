# Build artifacts
/build/
/build-aarch64/
/compile_commands.json
/ae-locker

# Per-arch release artifacts staged under dist/arch/<arch>/ae-locker
/dist/

# Encrypted test outputs (any local .ae-locked file)
*.ae-locked

# IDE / editor caches
/.vscode/
/.idea/
/.cache/
*.swp
*.swo
.DS_Store

# Sisyphus agent scratch (run-continuation state, etc.)
/.sisyphus/
