# Build artifacts
/build/
/build-aarch64/
/compile_commands.json
/lock

# Per-arch release artifacts staged under dist/arch/<arch>/lock
/dist/

# Encrypted test outputs (any local .locked file)
*.locked

# IDE / editor caches
/.vscode/
/.idea/
/.cache/
*.swp
*.swo
.DS_Store

# Sisyphus agent scratch (run-continuation state, etc.)
/.sisyphus/
